import { View, Text } from "react-native"

const MyCreatedRecipesScreen = () => {
    return (
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
            <Text>MyCreatedRecipes Screen</Text>
        </View>
    )
}

export default MyCreatedRecipesScreen;